# Mac
.DS_Store

# Build artifact — assembled by hand for Web Store upload, not source
ballpark.zip

# Playwright MCP scratch (screenshots/logs from manual debugging)
.playwright-mcp/

# Dev docs (not part of the shipped extension)
docs/

# Editor metadata
.vscode/
.idea/

# Node modules (if you ever add any)
node_modules/

# Local env files (defensive — shouldn't exist but just in case)
.env
.env.local
*.key